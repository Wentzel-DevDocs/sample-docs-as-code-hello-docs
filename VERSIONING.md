# Documentation versioning

See [Versions overview](./content/versions/overview.md) for the in-product explanation.

**Publish identity:** each broker push sets `manifest.version` from `VERSION` env or git SHA in CI.
