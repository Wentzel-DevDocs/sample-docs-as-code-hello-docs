{
  "name": "sample-hello-docs",
  "version": "1.1.0",
  "private": true,
  "description": "Simple enterprise sample docs-as-code repo (Hullo CLI) for hosted.devdocs.ai sample ingestion demos.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "check": "node scripts/check.mjs",
    "build": "DRY_RUN=1 node scripts/publish.mjs",
    "publish": "node scripts/publish.mjs",
    "test": "npm run check && npm run build"
  },
  "dependencies": {
    "marked": "^12.0.2",
    "sanitize-html": "^2.13.0"
  }
}
